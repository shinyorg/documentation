---
title: Fab & FabMenu
description: Material-style floating action button and expanding multi-action menu for .NET MAUI
---
import NugetDownloads from '/src/components/NugetDownloads.astro';
import PlatformSupport from '/src/components/PlatformSupport.astro';
import AiSkill from '/src/components/AiSkill.astro';

A Material Design-inspired floating action button (`Fab`) and a multi-action menu (`FabMenu`) that animates up from a main FAB with a staggered reveal. Each action supports `Icon`, `Text`, and `Command`, plus full color and border customization.

- <NugetDownloads name="Shiny.Maui.Controls" />
- <NugetDownloads name="Shiny.Blazor.Controls" />

<PlatformSupport frameworks={["maui", "blazor"]} />

| Closed | Menu Open |
|:---:|:---:|
| <img src="/images/fab/s1.png" width="220" alt="FAB Closed" /> | <img src="/images/fab/s2.png" width="220" alt="FAB Menu Open" /> |

## Placement

Place `Fab` / `FabMenu` inside a `Grid` that fills the page — the same pattern used by `SheetView` / `ImageViewer` — so the FabMenu's backdrop can cover the main content.

```xml
<ContentPage>
    <Grid>
        <!-- Main page content -->
        <ScrollView>
            <!-- ... -->
        </ScrollView>

        <!-- Fab/FabMenu overlay on top -->
        <shiny:FabMenu IsOpen="{Binding IsMenuOpen}"
                       Icon="plus.png"
                       HorizontalOptions="End"
                       VerticalOptions="End"
                       Margin="24">
            <!-- items -->
        </shiny:FabMenu>
    </Grid>
</ContentPage>
```

## AI Skill

<AiSkill skillName="shiny-controls" />
