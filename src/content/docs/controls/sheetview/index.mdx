---
title: Sheet View (Blazor Only)
description: Draggable sheet overlay (bottom or top) with configurable snap points for Blazor — the Blazor equivalent of MAUI's FloatingPanel
---
import NugetDownloads from '/src/components/NugetDownloads.astro';
import PlatformSupport from '/src/components/PlatformSupport.astro';
import AiSkill from '/src/components/AiSkill.astro';

`SheetView` is the Blazor equivalent of MAUI's `FloatingPanel`. It slides in from the bottom or top of the screen with configurable snap positions (detents), optional header peek when closed, backdrop dimming, pan gestures, and keyboard handling.

- <NugetDownloads name="Shiny.Blazor.Controls" />

<PlatformSupport frameworks={["blazor"]} />

## Screenshots

| Closed | Open at a detent | Header peek — bottom |
|:---:|:---:|:---:|
| <img src="/images/sheetview/blazor-s1.png" width="220" alt="Page with the sheet closed" /> | <img src="/images/sheetview/blazor-s2.png" width="220" alt="Sheet open at a detent with the backdrop dimmed" /> | <img src="/images/sheetview/blazor-s3.png" width="220" alt="Music player header peeking from the bottom edge while closed" /> |

| Peek expanded | Header peek — top | Top sheet expanded |
|:---:|:---:|:---:|
| <img src="/images/sheetview/blazor-s4.png" width="220" alt="The peeking music player expanded into the full sheet" /> | <img src="/images/sheetview/blazor-s5.png" width="220" alt="Weather summary peeking from the top edge while closed" /> | <img src="/images/sheetview/blazor-s6.png" width="220" alt="Top sheet expanded to show the full weather details" /> |

:::note
On MAUI, use [`FloatingPanel`](/controls/floatingpanel/) + `OverlayHost` instead. `SheetView` is only available in the Blazor package.
:::

## Setup

```razor
@using Shiny.Blazor.Controls
```

## Basic Usage

```razor
<div style="position: relative; height: 100vh;">
    <button @onclick="() => isOpen = true">Open Sheet</button>

    <SheetView IsOpen="@isOpen"
               IsOpenChanged="v => isOpen = v"
               SheetBackgroundColor="#FFFFFF"
               HandleColor="#CCCCCC"
               SheetCornerRadius="16">
        <div style="padding: 20px;">
            <h3>Sheet Content</h3>
            <p>Drag the handle or tap the backdrop to close.</p>
            <button @onclick="() => isOpen = false">Close</button>
        </div>
    </SheetView>
</div>

@code {
    bool isOpen;
}
```

## Custom Detents

```razor
<SheetView IsOpen="@isOpen"
           IsOpenChanged="v => isOpen = v"
           Detents="@detents"
           SheetBackgroundColor="#1E1E1E"
           HandleColor="#888888"
           SheetCornerRadius="24">
    <div style="padding: 20px; color: white;">
        <h3>Custom Sheet</h3>
    </div>
</SheetView>

@code {
    bool isOpen;
    List<DetentValue> detents = [new(0.33), new(0.66), new(1.0)];
}
```

## Header Peek

```razor
<SheetView IsOpen="@isOpen"
           IsOpenChanged="v => isOpen = v"
           ShowHeaderWhenMinimized="true">
    <HeaderTemplate>
        <div style="padding: 16px 16px 8px;">
            Now Playing — Click to expand
        </div>
    </HeaderTemplate>
    <ChildContent>
        <div style="padding: 20px;">
            <p>Full player controls here</p>
        </div>
    </ChildContent>
</SheetView>

@code {
    bool isOpen;
}
```

## Locked Sheet

```razor
<SheetView IsOpen="@isSignatureOpen"
           IsOpenChanged="v => isSignatureOpen = v"
           IsLocked="true"
           FitContent="true"
           HasBackdrop="true"
           SheetCornerRadius="20">
    <div style="padding: 20px;">
        <h3>Draw your signature</h3>
        <!-- signature content -->
        <button @onclick="() => isSignatureOpen = false">Done</button>
    </div>
</SheetView>

@code {
    bool isSignatureOpen;
}
```

## Properties

| Property | Type | Default | Description |
|---|---|---|---|
| `IsOpen` | `bool` | `false` | Show/hide the sheet (pair with `IsOpenChanged` for two-way binding) |
| `IsOpenChanged` | `EventCallback<bool>` | — | Fires when the sheet opens or closes |
| `ChildContent` | `RenderFragment?` | `null` | Content displayed in the sheet |
| `HeaderTemplate` | `RenderFragment?` | `null` | Custom header rendered above the content |
| `Detents` | `List<DetentValue>` | Quarter, Half, Full | Snap positions as ratios of available height |
| `SheetBackgroundColor` | `string` | `#FFFFFF` | CSS color of the sheet panel |
| `HandleColor` | `string` | `#CCCCCC` | CSS color of the drag handle |
| `SheetCornerRadius` | `double` | `16` | Top corner radius in pixels |
| `HasBackdrop` | `bool` | `true` | Show dimming backdrop behind the sheet |
| `CloseOnBackdropTap` | `bool` | `true` | Tap backdrop to close |
| `AnimationDuration` | `double` | `250` | Animation speed in ms |
| `ExpandOnInputFocus` | `bool` | `true` | Auto-expand when an input is focused |
| `IsLocked` | `bool` | `false` | Prevents swipe, pan, and backdrop dismiss; sheet can only be controlled programmatically |
| `FitContent` | `bool` | `false` | Measures content and auto-computes a single detent to fit it |
| `Location` | `SheetLocation` | `Bottom` | Where the sheet slides from (`Bottom` or `Top`) |
| `ShowHeaderWhenMinimized` | `bool` | `false` | Shows the header as a peek strip when the sheet is closed |

## Sizing sheet content

The sheet panel is always viewport-tall and is translated down to its detent. To keep the part below
the detent line from being laid out off-screen, the sheet publishes its on-screen height as the
`--sheet-visible-height` custom property on every transform — including mid-drag — and sizes its
content column off that. What that means when you write sheet content:

- Content that should fill the sheet takes `height: 100%`; it resolves against the **visible band**,
  not the viewport.
- Content taller than the band scrolls inside the sheet, so a trailing action row, a footer button or
  a `ChatView` input bar stays reachable even at a Quarter detent.
- Avoid a fixed pixel height picked for one detent — it will overflow the smaller ones.
- A full-bleed `HeaderTemplate` is clipped to `SheetCornerRadius`, so it keeps the rounded top edge.

### DetentValue

| Static Property | Ratio | Description |
|---|---|---|
| `DetentValue.Quarter` | `0.25` | 25% height |
| `DetentValue.Half` | `0.50` | 50% height |
| `DetentValue.ThreeQuarters` | `0.75` | 75% height |
| `DetentValue.Full` | `1.0` | Full height |

Custom detent: `new DetentValue(0.33)` for 33%.

:::note
Colors are CSS strings. Two-way binding uses the `IsOpen` / `IsOpenChanged` callback pattern. `Location` supports `Bottom` and `Top` (no `BottomTabs` on Blazor). Templates use Blazor `RenderFragment` instead of MAUI `DataTemplate`.
:::

## AI Skill

<AiSkill skillName="shiny-controls" />
