---
title: TableView Releases
tableOfContents: true
---

import RN from '/src/components/ReleaseNote.astro';

## Unreleased
<RN type="fix">**Drag-to-reorder now works.** `UseDragSort` was built on `DragGestureRecognizer`/`DropGestureRecognizer`, which are broken on Mac Catalyst, missing from the AppKit and GTK4 hosts, and expose no pointer position where they do work — a row could never be dropped anywhere meaningful. The gesture is now pan-driven on every platform: the row lifts under the finger, an insertion line marks the drop position, and dragging to the top or bottom edge auto-scrolls the table</RN>
<RN type="fix" platform="Android">Reorder drags were cancelled on their first frame — the enclosing `ScrollView` takes the gesture from its children at the touch slop (`ScrollOrientation.Neither` does not stop this; `MauiScrollView` only tests it in `OnTouchEvent`), and setting `ZIndex` on the dragged row makes MAUI remove and re-add the native view, dispatching `ACTION_CANCEL` to the view being dragged. The handle now requests no-intercept on touch-down and the row is raised with a native `BringToFront`</RN>
<RN type="fix" platform="iOS">`UIScrollView` cancelled the row's pan once its own pan recognizer engaged; the scroller is now disabled for the duration of the touch. `ScrollView.Orientation` is deliberately not used for this — toggling it on iOS snaps the scroll offset back to the top</RN>
<RN type="enhancement">`ItemDroppedEventArgs` gains `Item`, the moved row's binding context, for reordering sections whose rows come from `ItemsSource`. `FromIndex`/`ToIndex` are positions among the section's rendered rows</RN>

## 1.1 - March 30, 2026
<RN type="feature">New `DurationPickerCell` for duration/timespan input with min/max constraints, customizable format, and prompt-based entry</RN>
<RN type="enhancement">EntryCell now renders borderless on all platforms using MAUI handler customization via `UseShinyTableView()` — no more visible platform borders on the inline text entry</RN>
<RN type="info">The library now includes platform targets for Android and iOS/MacCatalyst solely for borderless Entry rendering. All controls and cells work on **any** .NET MAUI platform — the platform targets are only used to clean up Entry appearance on those specific platforms.</RN>

## 1.0.2 - February 24, 2026
<RN type="fix" platform="iOS">Section header and footer text is now readable in dark mode using iOS secondary label colors</RN>
<RN type="enhancement">EntryCell now uses a clean named Style (transparent background, no border) instead of inline property settings</RN>

## 1.0.1 - February 19, 2026
<RN type="fix" platform="iOS">DatePickerCell and TimePickerCell now use Focus() on iOS/Mac instead of transparent overlay, fixing tap interaction</RN>
<RN type="enhancement">Section header background now always applies a theme-aware default (light: #F2F2F7, dark: #1C1C1E) instead of only when explicitly set</RN>
<RN type="enhancement">Separator colors now use iOS system separator colors by default instead of low-opacity BoxView</RN>
<RN type="enhancement">Drag-sort reordering replaced up/down arrow buttons with native drag-and-drop gesture recognizers</RN>

## 1.0 - February 21, 2026
<RN type="feature">Initial Public Release</RN>
<RN type="feature">14 built-in cell types: LabelCell, CommandCell, ButtonCell, SwitchCell, CheckboxCell, SimpleCheckCell, RadioCell, EntryCell, DatePickerCell, TimePickerCell, TextPickerCell, NumberPickerCell, PickerCell, and CustomCell</RN>
<RN type="feature">Three-level cascading style system (TableView, Section, Cell)</RN>
<RN type="feature">Dynamic sections and cells via ItemsSource / ItemTemplate with INotifyCollectionChanged support</RN>
<RN type="feature">Drag and sort reordering within sections</RN>
<RN type="feature">RadioCell with section-scoped or TableView-scoped radio groups</RN>
<RN type="feature">PickerCell with full-page single or multi-select and auto-generated value text</RN>
<RN type="feature">Pure .NET MAUI implementation — works on all MAUI platforms</RN>
<RN type="feature">Full MVVM support with BindableProperty on all properties</RN>
<RN type="feature">Programmatic scroll control</RN>
<RN type="feature">Section headers and footers with text or custom views</RN>
<RN type="feature">Cell icons, hints, descriptions, and borders</RN>
<RN type="feature">Selection highlighting with customizable colors</RN>
