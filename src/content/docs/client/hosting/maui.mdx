---
title: MAUI Hosting
description: The recommended way to integrate Shiny with .NET MAUI
---
import { Steps, Card, CardGrid } from '@astrojs/starlight/components';
import NugetBadge from '../../../../components/NugetBadge';

<Card title="Recommended for most apps" icon="approve-check">
.NET MAUI is the recommended hosting model for Shiny. A single line of code wires up all platform lifecycle hooks, dependency injection, and background services — so you can focus on registering your Shiny modules.
</Card>

## Setup

<Steps>
1. **Install the NuGet package**

   <NugetBadge name="Shiny.Hosting.Maui" />

2. **Add `UseShiny()` to your `MauiProgram.cs`**

   ```csharp
   using Shiny;

   namespace ShinyApp;

   public static class MauiProgram
   {
       public static MauiApp CreateMauiApp()
       {
           var builder = MauiApp
               .CreateBuilder()
               .UseMauiApp<App>()
               .UseShiny() // <-- this wires all Shiny lifecycle through MAUI
               .ConfigureFonts(fonts =>
               {
                   fonts.AddFont("OpenSans-Regular.ttf", "OpenSansRegular");
                   fonts.AddFont("OpenSans-Semibold.ttf", "OpenSansSemibold");
               });

           // Register your Shiny services here
           // builder.Services.AddGps<MyGpsDelegate>();
           // builder.Services.AddJob(typeof(MyJob));

           return builder.Build();
       }
   }
   ```

3. **Register Shiny services**

   Use `builder.Services` to register any Shiny modules you need — GPS, Bluetooth, Jobs, Notifications, etc. See each module's Getting Started page for specifics.
</Steps>

:::tip
This is all you need. `UseShiny()` handles all Android lifecycle callbacks, iOS delegate methods, and Windows integration automatically. No platform-specific boilerplate required.
:::

## When to use a different hosting model

| Scenario | Hosting Model |
|----------|---------------|
| Standard .NET MAUI app | **MAUI** (this page) |
| .NET native without MAUI (iOS/Android) | [Native](/client/hosting/native) |
| Existing app with complex platform setup | [Manual](/client/hosting/manual) |
